import AuthPhoneNumber from "./AuthPhoneNumber";

const FindUserId = () => {
  return (
    <div className="findId-container">
      <AuthPhoneNumber />
    </div>
  );
};

export default FindUserId;
